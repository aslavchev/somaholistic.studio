SOMA STUDIO - 5-Star Improvement Prompts for Lovable
====================================================

Copy these prompts into Lovable.dev one at a time.


CRITICAL FIXES (Do First!)
--------------------------

PROMPT 0.1: Fix Desktop ServiceCard

Problem: "Виж повече" button doesn't work on computers.

Copy this into Lovable:

Fix ServiceCard.tsx so expand/collapse works on both desktop and mobile:

1. Line 69 - Make card clickable on all devices:
   Change: onClick={isMobile && hasExpandedContent ? onToggle : undefined}
   To: onClick={hasExpandedContent ? onToggle : undefined}

2. Line 115 - Show expanded content on all devices:
   Change: {isMobile && isExpanded && (
   To: {isExpanded && (

3. Line 237 - Show toggle button on all devices:
   Change: {isMobile && hasExpandedContent && (
   To: {hasExpandedContent && (

4. Delete lines 256-268 (the decorative span that doesn't work)

5. Lines 107-111 - Show short description when collapsed:
   Change: {(!isMobile || !isExpanded) && (
   To: {!isExpanded && (

Keep hover effects but make click the main interaction.

---

PROMPT 0.2: Security - Sanitize Form Inputs

Copy this into Lovable:

In BookingDialog.tsx, sanitize the WhatsApp message:

1. Add sanitize function at top of component:
   const sanitizeInput = (input) => {
     return input.replace(/[<>]/g, '').trim();
   };

2. In handleSubmit, wrap user inputs with sanitizeInput():
   - sanitizeInput(formData.name)
   - sanitizeInput(formData.email)
   - sanitizeInput(formData.phone)
   - sanitizeInput(formData.service)

This prevents potential security issues.

---

PROMPT 0.3: Google Maps Integration

Copy this into Lovable:

Add Google Maps link and review badge:

1. In src/lib/constants.ts, add:
   GOOGLE_MAPS: "https://maps.app.goo.gl/WvbNBxrZhq6E9xfP8"

2. In Testimonials.tsx, make first trust badge clickable:
   - Link to CONTACT.GOOGLE_MAPS
   - Text: "5.0 в Google Maps" / "5.0 on Google Maps"
   - Opens in new tab

3. In Contact.tsx, add "Get Directions" link:
   - Link to CONTACT.GOOGLE_MAPS
   - Text: "Вижте на картата" / "Get Directions"
   - Use MapPin icon from lucide-react

---

PROMPT 0.4: Structured Data for SEO

Copy this into Lovable:

Add LocalBusiness structured data to index.html inside head tag:

Add a script tag with type="application/ld+json" containing:
- Business name: SOMA Wellness Studio
- Address: ул. 409-та 13, Манастирски Ливади Изток, София
- Phone: +359888333424
- Coordinates: 42.6597861, 23.296335
- Opening hours: Mon-Sun 09:00-19:00
- Services: SOMA Ritual, Thai Massage, Classical Massage
- Links to Instagram and Google Maps

This helps Google show your business info in search results.


TIER 1: HIGH IMPACT
-------------------

PROMPT 1.1: Section Reorder

Copy this into Lovable:

Reorder sections in Index.tsx so About appears before Services:

Current: Hero → Services → About → Testimonials → Gallery → Contact
New: Hero → About → Services → Testimonials → Gallery → Contact

This builds trust before showing prices.

---

PROMPT 1.2: Smooth Scroll Navigation

Copy this into Lovable:

Add smooth scroll behavior:

1. In src/index.css, add to html:
   scroll-behavior: smooth;

2. Add to each section:
   scroll-margin-top: 80px;

This makes navigation feel polished.

---

PROMPT 1.3: Therapist Photo Note

Copy this into Lovable:

In About.tsx, add a comment above the therapist image:
/* TODO: Replace with actual therapist photo */

This reminds the team to add the real photo before launch.


TIER 2: DELIGHT FEATURES
------------------------

PROMPT 2.1: Carousel Navigation Dots

Copy this into Lovable:

Add navigation dots to Testimonials carousel:

1. Track selected index with useState
2. Use emblaApi.on('select') to update index
3. Add dots below carousel, before trust badges
4. Active dot: bg-primary, inactive: bg-muted
5. Make dots clickable to jump to testimonial

---

PROMPT 2.2: Add More Testimonials

Copy this into Lovable:

Add 2 more testimonials to Testimonials.tsx:

Testimonial 4:
- Name: Ивана К. / Ivana K.
- Service: Фитотерапия / Phytotherapy
- Quote: "Билковият план напълно промени енергията ми. След месец вече нямам проблемите, с които се борих години."

Testimonial 5:
- Name: Петър В. / Petar V.
- Service: Класически масаж / Classical Massage
- Quote: "Всяка седмица идвам тук след работа. Това е моят ритуал за възстановяване."

Update trust badge to: "4.9/5 от 150+ отзива"

---

PROMPT 2.3: Gallery Lightbox Navigation

Copy this into Lovable:

Add arrow navigation to Gallery lightbox:

1. Add ChevronLeft and ChevronRight icons
2. Add Previous/Next buttons on sides of image
3. Add keyboard support (arrow keys)
4. Show image counter: "1 / 4"


TIER 3: FINAL POLISH
--------------------

PROMPT 3.1: Skip to Content Link

Copy this into Lovable:

Add accessibility skip link in Index.tsx as first element:

- Link to #services
- Hidden by default (sr-only class)
- Visible when focused
- Text: "Към съдържанието" / "Skip to content"

---

PROMPT 3.2: Button Loading State

Copy this into Lovable:

Add loading state to booking confirm button:

1. Add isSubmitting state
2. Show Loader2 spinner when submitting
3. Change text to "Изпращане..." / "Sending..."
4. Disable button while submitting

---

PROMPT 3.3: Meta Tags and SEO

Copy this into Lovable:

Add meta tags to index.html:

- Title: SOMA STUDIO - Холистичен център за благополучие в София
- Description: Професионални масажи, тайландски масаж, енергийна терапия
- Keywords: масаж София, тайландски масаж, wellness
- Open Graph tags for Facebook/Instagram sharing
- Twitter card tags

---

PROMPT 3.4: Service Category Badges

Copy this into Lovable:

Add category badges to ServiceCard:

- SOMA Ritual: "Авторска" (Signature) - primary color
- Massages: "Масаж" (Massage) - blue
- Energy Therapy: "Терапия" (Therapy) - purple
- Facial: "Красота" (Beauty) - pink
- Coaching: "Коучинг" (Coaching) - green

Show badge in top-right corner of card.


PRIORITY ORDER
--------------

Critical (Do First):
1. Fix desktop ServiceCard (0.1)
2. Security sanitize inputs (0.2)
3. Google Maps integration (0.3)
4. Structured data SEO (0.4)

High Impact:
5. Section reorder (1.1)
6. Smooth scroll (1.2)
7. Therapist photo note (1.3)

Delight:
8. Carousel dots (2.1)
9. More testimonials (2.2)
10. Gallery arrows (2.3)

Polish:
11. Skip to content (3.1)
12. Button loading (3.2)
13. Meta tags (3.3)
14. Category badges (3.4)


BEFORE LAUNCH CHECKLIST
-----------------------

[ ] Replace therapist placeholder with Mari's real photo
[ ] Export logo as PNG (without phone app screenshot)
[ ] Test booking flow end-to-end
[ ] Verify WhatsApp messages arrive correctly
[ ] Test on mobile devices
[ ] Run Lighthouse audit (aim for 90+ scores)
[ ] Set up custom domain
[ ] Configure SSL certificate


QUESTIONS FOR MARI
------------------

1. What's your exact Google rating?
2. Are business hours Mon-Sun 09:00-19:00?
3. Any services or prices to update?
4. Do you want review capability on site?
